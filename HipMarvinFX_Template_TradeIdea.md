TEMPLATE — TRADE IDEA
v1 · Saved Week 29 · Reusable per Rule 12 precedent — do not regenerate weekly, fill in per idea
═══════════════════════════════════════════════════════════════════
[One block per Trade Priority List entry. Fields left blank must stay blank/Pending — never filled with a plausible-sounding value, per Rule 19.]

## {Priority N — Pair Direction (Range/Reactive/etc.) — ★☆☆☆☆}

**Lifecycle stage (Rule 20):** Created / Evidence Registered / Validated / Triggered / Active / Protected / Partial TP / Closed / Audited / Archived — [state current stage explicitly]

**Levels**
- Entry:
- TP1:
- TP2:
- Stop:
- R:R:

**Liquidity Rule fields (Rule 15):**
- Liquidity Source (buy-side/sell-side):
- Liquidity Objective (internal/external):
- Expected delivery path (e.g. sweep → retrace into premium → mitigate OB → reject FVG → attack sell-side liquidity):

**Market Structure Hierarchy check (Rule 17 — higher timeframe wins, fixed in advance):**
- Weekly Structure (macro bias):
- Daily Structure (confirms/corrects, cannot override):
- H4 Structure (execution zone):
- H1 Structure (refines H4 zone):
- M15/M5 (trigger only, never bias):
- Conflict flag: [state Yes/No — if lower TF conflicts with governing higher TF, downgrade conviction or mark Unknown per Rule 18, do not quietly override]

**Trade Priority Orderflow Evidence Block (Rule 15, in this order — chart-sourced only, per Rule 3 extension):**
- Macro Bias:
- Weekly Structure:
- Daily Structure:
- External Liquidity:
- Premium/Discount (per active dealing range, weekly > daily > 4H):
- OTE Zone (only if aligned with macro bias + structure + session timing — never standalone):
- Order Block:
- Fair Value Gap:
- Session Alignment:
- Conviction (Rule 16 — Exceptional / High / Moderate / Low / Observation Only — never numeric unless mechanical rubric exists and is documented):

**Invalidation (Rule 9 + Week 29 structural upgrade):**
- Price-based invalidation:
- Structural invalidation (OB failure / FVG inversion / liquidity objective reached / MSS / premium array reclaimed / discount array lost):

**Observation vs. Interpretation (Rule 18 — narrative written only after evidence block above is complete):**
- Observation (what is directly visible, no inference):
- Interpretation (explicitly flagged as inference):

**Rule 8 Carryover conflict check:**
- Directly opposite an already-open prior-week position? [Yes/No — if Yes, name the position, entry, stop, floating figure]
- Same-direction stack on an already-open position? [Yes/No — if Yes, name it]

**Decision Log link (Rule 23):**
- Thread-ID:

**Evidence Register status (Rule 21):** [see companion Evidence Register template — must be Validated before this idea publishes]

**Reasoning:** [prose — written last, after every field above is filled or explicitly marked Pending/Unknown/N/A]

───────────────────────────────────────────────────────────────────
END TEMPLATE — TRADE IDEA
