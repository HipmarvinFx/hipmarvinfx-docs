TEMPLATE — SCENARIO MATRIX
v1 · Saved Week 29 · Reusable per Rule 12 precedent — do not regenerate weekly, fill in per event
═══════════════════════════════════════════════════════════════════
[One block per red-folder event, per Rule 7. Header must be the exact, literal FF calendar-event title, cross-checked once at construction — no blockquote syntax inside the section, per Rule 10. If constructed after the event, add a Backfill note.]

## {A6x. Event Title — Day · Date · Time WAT}

{Also covers: [any joint/grouped events per Rule 5, stated explicitly]}
{Forecast X% · Prior Y% · one-line framing of what's actually at stake}

{Backfill note, if applicable: constructed after the fact on [date/time] using [source].}

- **Branch 1 — {Label} ({X}% probability):** [Condition that defines this branch, in figures not vibes.] [What moves, which pairs, in which direction.] [What this confirms or invalidates from the Trade Priority List — name the specific Priority N entries affected.]
- **Branch 2 — {Label} ({Y}% probability):** [Same structure as Branch 1.]
- *(Branch 3 if the event genuinely has three distinct outcomes — do not force a binary split where a third outcome exists.)*

(Probability split sums to 100%.)

**Probability rationale (Rule 16):** [brief identification of what the split is based on — macro conditions, positioning, structure, historical behavior, event characteristics. Stated as analytical estimate, not statistical certainty.]

**Evidence hierarchy note (Rule 16, if conflicting inputs exist):** [state which evidence tier — verified release > timestamped chart > confirmed structure > positioning data > analyst interpretation — is being weighted, and why the lower tier isn't overriding it.]

───────────────────────────────────────────────────────────────────
END TEMPLATE — SCENARIO MATRIX
